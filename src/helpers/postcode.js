// Axios for making requests
import axios from "axios";

const config = require("./config.json");

export function checkPostcodes(postcode_list, callback) {
  callback();
}

export function sortPostcodes(postcode_list, callback) {
  callback();
}

export function extractPostcodeSectors(postcode_list) {
  return sectors;
}

export function extractPostcodeSector(postcode) {
  return sector;
}

export function extractPostcodeDistrict(postcode) {
  return district;
}

export function extractPostcodeArea(postcode) {
  return area;
}

export function getLsoasFromPostcodeSectors(sectors) {
  callback();
}

export function getLsoasFromPostcodeSectors(sectors) {

  axios.post(config.postcode_api)
  callback();
}